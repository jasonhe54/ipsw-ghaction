"Ask a question about the current camera frame: ${prompt}" = "Ask a question about the current camera frame: ${prompt}";
"Ask a follow-up question about the current camera frame: ${prompt}" = "Ask a follow-up question about the current camera frame: ${prompt}";
"Search for ${searchTerm} in the current camera view." = "Search for ${searchTerm} in the current camera view.";
"Detect ${detectionType}" = "Detect ${detectionType}";
"Start ${activity}" = "Start ${activity}";
